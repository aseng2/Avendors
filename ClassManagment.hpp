#include <iostream>
#include <string>

class Managment {
    public:
    Managment();
    void displayPrice(std::string input);       // for when customer enter display #
    bool sale (int payment);                    // for when customer inserts money to buy stuff
    void restocking();                          // for when Restocker needs to restock
    bool turnOff(std::string input);            // to shut off machine

// I really don't know exactly what this was supposed to do so i just put these 


};